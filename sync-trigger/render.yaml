services:
  - type: web
    name: livite-crm-sync
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: GITHUB_PAT
        sync: false  # Set manually in Render dashboard
